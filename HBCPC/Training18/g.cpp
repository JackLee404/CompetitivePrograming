#include <iostream>
#define rep(i, j, k) for (int i = j; i <= k; i ++)
using namespace std;

int n, m, q;

int main() {
	cin >> n >> m >> q;
	while (q --) {
		int a, b;
		cin >> a >> b;
	}
	cout << n * m;
}