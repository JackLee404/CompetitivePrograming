#include <iostream>
#include <stack>
#define rep(i, j, k) for (int i = j; i <= k; i ++)
using namespace std;

const int N = 1e3 + 10;

int n, m, k, cnt = 0;

int a[N];

int main() {
	cin >> n >> m >> k;
	cnt = n;
	rep (i, 1, n) {
		cin >> a[i];
	}
	int n_top = -1;
	while (n != 0) {
		if (n_top == -1) {
			
		}
	}
}