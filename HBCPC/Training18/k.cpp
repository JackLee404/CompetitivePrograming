#include <iostream>
#include <vector>
#include <algorithm>
#define rep(i, j, k) for (int i = j; i <= k; i ++)
#define per(i, j, k) for (int i = j; i >= k; i --)
using namespace std;

struct node{

};

int main() {
	int n, m;
	cin >> n >> m;	
	rep (i, 1, n) {

	}
}