#include <iostream>
#include <map>
#include <cstring>
#include <string>
#include <cctype>
#define rep(i, j, k) for (int i = j; i <= k; i ++)
using namespace std;

const int N = 300;
map<int, bool> mp;

int main() {
    int n, a[N];
    while (cin >> n) {
        rep (i, 1, n) {
            cin >> a[i];
        }

        for (int i = 0; i <= n; i ++) {
            for (int j = 0; j <= n; j ++) {
                if (i == j)
                    continue;
                if ()
            }
        }
    }
}
